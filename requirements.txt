# DUAG-SLAM Requirements
# ======================
# Install with: conda activate duag && pip install -r requirements.txt

# Core
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scipy>=1.10.0

# 3DGS rendering (will also need to compile diff-gaussian-rasterization from extracted/)
plyfile>=0.8
opencv-python>=4.8.0

# Distributed optimization
pybind11>=2.11.0    # For DPGO C++ bridge

# Evaluation metrics
scikit-learn>=1.3.0  # For calibration metrics
lpips>=0.1.4         # Perceptual image quality
open3d>=0.17.0       # Point cloud processing

# Experiment management
pyyaml>=6.0
wandb>=0.15.0        # Experiment tracking (optional but recommended)
matplotlib>=3.7.0
tqdm>=4.65.0

# Dataset loading
h5py>=3.9.0          # For some dataset formats
pillow>=10.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Code quality
black>=23.7.0
ruff>=0.0.280
